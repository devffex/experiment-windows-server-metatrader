[project]
name = "shared-schemas"
version = "0.1.0"
description = "Shared Pydantic data schemas for multi-user MetaTrader platform"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
